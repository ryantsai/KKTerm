# 12 — IT Ops

## AI grep hints

- Keys: `itops.*`, `settings.sectionItOps`, `watchdog.*`
- Topics: IT Ops Module, Sites, Site View, Server Room, Server Room View, Server Room floor plan, Server Room 2.5D view, Server Room blackout, Konami sequence, Rack View, racks, Rack Device, Rack Device Type, Rack Device Properties, rack unit (U), rack shell, tree navigator, Task Library, reusable Task, drill-down, virtual datacenter, click-to-connect, Hosts, Host inventory, host import, hostname list, connectivity scan, child hosts, VM guests, container guests, Host Connection bindings, Batch Runs, Automations, run history, Run Report, transport, SSH, WinRM, PsExec, trigger, condition, action, armed, disabled, Watchdog Status Bar
- Tutorial targets: `app.activityRailItOps`, `itops.sitesTree`, `itops.siteView`, `itops.hostsPanel`, `itops.hostsRunTask`, `itops.hostsImport`, `itops.hostsScan`, `itops.automationsPanel`, `itops.automationsNew`, `itops.runHistoryPanel`, `itops.taskLibrary`, `itops.taskLibraryNew`
- Synonyms: "run on many hosts", "bulk command", "site", "host group" (renamed to Site), "host collection", "host inventory", "import servers", "detect ssh", "detect winrm", "scan hosts", "virtual machines on a host", "rack diagram", "rack elevation", "top-down view", "floor plan", "floor-plan view", "room footprint", "DCIM map", "2.5D view", "isometric room", "3D server room", "axonometric view", "utilization heatmap", "capacity heatmap", "rack health map", "virtual datacenter", "data center map", "open from rack", "scheduled monitor", "saved watchdog", "automation rule", "batch script", "run report"

The **IT Ops Module** is an Activity Rail destination for operating across multiple existing Connections. Its left operational navigator contains Sites and the global `itops.tasks.heading` Task Library. Expanding a Site exposes `itops.navigation.serverRooms`, `itops.tabs.hosts`, `itops.tabs.autos`, and `itops.navigation.runHistory`; only the active Site normally needs to remain expanded. Settings → General → `settings.activityRail` controls whether the Module appears; it is visible by default.

## Sites

A **Site** is the durable top-level container for Server Rooms and Racks. It is not itself a Connection and owns no live Session or secret. When IT Ops has no Site data, KKTerm keeps an undeletable Default Site so the topology always has a safe parent container.

Open Sites from the add menu beside `itops.title` and choose `itops.racks.addSite`. The New Site dialog explains that a Site can model a region, location, customer, project, or other infrastructure grouping. Enter `itops.sites.nameLabel`; Connection binding is not part of Site creation. The header's shared icon picker customizes the Site icon. Save with `itops.actions.create`.

The left column includes the `itops.title` label/icon and can be resized like the Connection Tree. Dragging its divider tracks the pointer directly, and the chosen width persists when the drag ends. Use the title-bar button immediately before `app.aiAssistant` to hide or show it. Site children are predefined navigation destinations, not stored folders. `itops.navigation.serverRooms` preserves the existing Server Room → Rack topology tree; `itops.tabs.hosts`, `itops.tabs.autos`, and `itops.navigation.runHistory` each open their own Site-scoped page. `itops.tasks.heading` is global and therefore appears once outside all Sites.

### Site View, Server Room View, and Rack View

The Site topology is a Connection-tree-style navigator. The left panel nests **Site → Server Room → Rack** as collapsible, searchable (`itops.racks.treeSearchPlaceholder`) rows, with `connections.collapseAll` and `connections.expandAll` controls beside `itops.sites.heading` when expandable rows exist. When the virtual `itops.navigation.serverRooms` row is selected, an `itops.racks.sortAction` icon button appears immediately to the left of those controls; its `itops.racks.sortAscending` and `itops.racks.sortDescending` choices naturally order that Site's Server Room names and persist the direction for the Site. Selecting an individual Server Room shows the same icon and choices, naturally orders only that room's Rack names, and persists the direction for that Server Room without changing Rack placement in the room views. Clicking a **Site** or `itops.navigation.serverRooms` opens the overview-only **Site View**, a free-form placement surface for its **Server Room** cards: each card sits at its freely chosen position in normal and edit mode alike, while the dot placement grid appears only in edit mode. Site View has no segmented control; Hosts, Automations, and Run History are separate navigator destinations. Site View edit mode also shows the same right-side `itops.floorPlan.pickerTitle` column used by Server Room View, with one `itops.racks.serverRoomLabel` card that opens the existing New Server Room dialog for the selected Site. When a Site has no Server Rooms, normal mode shows `itops.sites.emptyServerRoomsHint` with an inline New Server Room action; edit mode keeps the empty placement grid and Server Room picker visible. Right-clicking the virtual `itops.navigation.serverRooms` row opens a native menu whose `itops.racks.addServerRoomAction` command creates a Server Room under that Site and whose `itops.racks.sortAction` submenu exposes the same two ordering choices. Right-clicking a Site, Server Room, or Rack row opens a native command menu whose final item is always `common.properties`. Site shows `itops.actions.delete`, a separator, then Properties. Rack adds `itops.actions.duplicate` before Delete. Server Room prepends `itops.racks.addRackAction` and the `itops.racks.sortAction` submenu, then shows Duplicate before Delete. Duplicate opens the existing Properties dialog with the next available `#N` name beginning at `#2` and the copied settings prefilled; Save creates the independent deep copy and Cancel creates nothing. A Rack copy includes its properties, background, and Rack Devices but starts without overlapping room coordinates, while a Server Room copy includes its finish, icon, background, room objects, Racks, Rack Devices, and internal layout. Properties reopens that entity's add dialog in edit mode; Server Room Properties also owns its durable `itops.floorPlan.floorColorLabel` finish. Delete opens the matching confirmation (`itops.sites.deleteTitle`, `itops.racks.deleteServerRoomTitle`, or `itops.racks.deleteTitle`); the Default Site's Delete item is disabled. Site View, Server Room View, and Rack View each have an icon-only `itops.actions.viewActions` toolbar: `itops.actions.edit` / `itops.actions.editDone` toggles placement editing, and `itops.actions.export` opens export options. PDF export creates a graphical topology report with room summaries, scaled rack elevations and placed Rack Device faceplates, followed by paginated inventory data; it is not a text-only listing. In Site View, edit mode lets Server Room cards be dragged to new positions or removed with a confirmation (`itops.racks.deleteServerRoomTitle`); the toolbar's `itops.sites.autoOrganize` action snaps every card back onto the default grid arrangement; right-clicking empty surface exposes `itops.racks.changeBackground`, which opens the shared background dialog with the same image/SVG/video media support as Dashboard and persists the Site background; export offers `itops.export.pdf`.

Clicking a **Server Room** opens **Server Room View**, showing its racks grouped under their `itops.racks.groupLabel` tag (`itops.racks.ungrouped` for untagged racks). When a Server Room has no Racks, centered guidance (`itops.racks.emptyServerRoomHint`) includes an inline action that opens the existing Rack dialog with that Server Room selected. Server Room View has three layouts, toggled with the icon-labelled `itops.floorPlan.viewLabel` control centered in the same top toolbar row as the `itops.actions.viewActions` buttons: `itops.floorPlan.viewElevation` (the default rack-elevation rows), `itops.floorPlan.viewFloor`, a blueprint-style top-down floor plan, and `itops.floorPlan.view25d`, a 2.5D axonometric room. In elevation mode, hovering a cabinet reveals its single flip icon immediately above the top-right ceiling corner (`itops.racks.showFace`); a transparent hover corridor covers the gap to the outside control so the pointer can reach it without hiding it. The button turns only that Rack between `itops.racks.face.front` and `itops.racks.face.rear` with a short depth-turn animation without competing with a device's edit controls, while the `itops.racks.allRackFacesLabel` icon immediately left of Edit continues to flip every visible rack together. The header's online/warning/offline tallies cover cabinet devices on both mounting faces even though its device-count subtitle describes only the visible face. Rear elevations move their U-number gutter to the right, use a subtle cable-side texture, and show the back artwork of a rack-top 乖乖 package so the viewed side remains legible without relying on color. Every rack reserves room above the cabinet for the package; edit mode shows the same right-side `itops.floorPlan.pickerTitle` Rack Device picker column Rack View uses — the armed device snaps to the currently visible mounting face of the cabinet nearest the pointer, so one flow adds devices to any rack in the room — and enables Rack Device drag/drop and direct delete controls on Racks and Rack Devices, while normal mode does not expose drag/drop targets. The floor plan and the 2.5D view share one floor grid: each Rack stands on a grid cell with a stored facing, so moving or rotating a Rack in either view rearranges both. There is no metric toggle; every Rack footprint/cabinet is painted in the Rack's shell finish (the same colour on the whole cabinet — no status colouring; health, the worst placed-device status, reads as text — `itops.floorPlan.health.ok` / `itops.floorPlan.health.warning` / `itops.floorPlan.health.critical`, with `itops.floorPlan.health.empty` for an empty rack — in the footprint tooltip and the hover detail card both spatial layouts share) and carries compact always-on tags: the device count and summed Rack Device power draw (`itops.floorPlan.powerValue` against the Rack dialog's optional `itops.racks.powerCapacityLabel` watts, or `itops.floorPlan.powerDrawOnly` when no capacity is set; each Rack Device's optional `itops.racks.powerDrawLabel` watts sums into it). Rack heights and unit occupancy are never written on the view itself; they appear only in the hover detail card. Racks in adjacent cells connect side-to-side with no gap, like a real rack row; the facing edge on the plan shows which way the front points. A Rack footprint's displayed depth follows the Rack's `itops.racks.depthLabel` millimetres against the 1200 mm floor cell — 600 mm reads as half a cell, 1200 mm as the whole cell, values in between scale by ratio, and any deeper custom depth still draws as one full cell — with the front face always flush on the cell borderline the facing points at. Each 2.5D cabinet carries its Rack name as a label fixed to the cabinet top and rotated with the Rack facing; its physical front and rear faces each render only the Rack Devices mounted on that side as miniature faceplate strips with status LEDs. The room can be viewed from four fixed corners with the `itops.floorPlan.viewAngleLabel` stepper (`itops.floorPlan.rotateViewLeft` / `itops.floorPlan.rotateViewRight`, curved-arrow buttons). Both spatial layouts grow their floor grid to cover the whole view, and each carries a vertical ruler-style zoom control (`itops.floorPlan.zoomLabel`) floating over the room's top-right corner: one clickable tick per fixed level between 50% and 200% (`itops.floorPlan.zoomSet`), and scrolling the mouse wheel over the room steps the active tick the same way. The 2.5D floor is a solid finish (never transparent) selected in Server Room Properties from `itops.floorPlan.floorColor.default`, `itops.floorPlan.floorColor.concrete`, `itops.floorPlan.floorColor.graphite`, `itops.floorPlan.floorColor.green`, or `itops.floorPlan.floorColor.blue`; it persists with that Server Room. Each spatial layout's zoom level persists locally and is restored the next time that layout opens. When the room is larger than the window, it pans with a held left-button drag on the floor, a middle-mouse drag from anywhere, or, after clicking the room, the arrow keys. Hovering a floor-plan footprint or a 2.5D cabinet shows the same compact detail card with health, utilisation, power, used/total rack units, device count, and status tallies; the card flips and stays inside the visible room at every edge instead of being clipped. Clicking a footprint or cabinet drills into that Rack outside edit mode. Right-clicking empty elevation or 2.5D space exposes `itops.racks.changeBackground`, which opens the same shared background dialog used by Dashboard and persists the selected Server Room background.

Both spatial layouts also hold **room objects** — non-rack fixtures picked from the edit-mode `itops.floorPlan.pickerTitle` column, a full-height searchable (`itops.floorPlan.pickerSearchPlaceholder`) grid of preview cards on the right of the room holding a `itops.floorPlan.pickerRack` card plus `itops.floorPlan.object.camera`, `itops.floorPlan.object.aircon`, `itops.floorPlan.object.fireExtinguisher`, `itops.floorPlan.object.ups`, `itops.floorPlan.object.sensor`, `itops.floorPlan.object.smokeDetector`, `itops.floorPlan.object.crashCart`, `itops.floorPlan.object.wall`, and `itops.floorPlan.object.kuaikuai`. Room objects draw only their artwork; no type icon floats above the object in either mode. Clicking a card arms one item: a room fixture follows the mouse immediately, while the `itops.floorPlan.pickerRack` card first opens the New Rack dialog to collect its properties. The armed Rack or fixture remains visible under the mouse anywhere in the app content, snaps to the hovered floor cell, and previews its position without a direction marker. The first valid click locks that position and shows a high-contrast facing arrow just outside the object; moving the pointer around it moves the arrow among four sides and turns the preview to match. The second valid click commits both position and facing. A blocked position or facing stays armed for another choice, with the highlight and arrow turning red. After the second click, the Wall card stays armed for continuous wall placement, while other fixture cards disarm. Right-click anywhere, press Escape, choose the armed picker card again, select another app control outside the room canvas, exit edit mode, change layout, or navigate away to cancel; cancellation discards a configured but still-unplaced Rack. In the picker's New Rack dialog, `itops.racks.sequenceAction` inserts the literal Java-style numeric token `%02d` into the Rack name. A name such as `TP-N%02d` starts with one more than the highest matching Rack number already in that Server Room (`TP-N11` produces `TP-N12`), then keeps the same Rack settings armed and creates the next incremented Rack after each completed two-click placement until cancellation. A Rack name without `%02d` remains a one-shot placement.

In either spatial layout, Shift-clicking an existing Rack or room object arms a temporary deep copy with the source facing and configuration already fixed. Its cursor ghost follows immediately, and one normal click on another blank floor cell commits it. A copied Rack receives the next available `#N` name and includes its Rack Devices. Right-click, Escape, leaving edit mode, switching layouts, navigating away, or selecting another app control cancels the temporary copy and creates nothing.

Room object footprints follow their real size against the floor cell: small fixtures (camera, fire extinguisher, sensor, smoke detector, 乖乖 pack) occupy one quarter of a cell anchored to a cell corner — in both the floor plan and 2.5D view, the placement pointer's quadrant picks the corner — while larger fixtures remain centered in their snapped cell and use their real footprint. An object also has a vertical position in rack units, so several occupants can share one floor cell as long as their vertical spans don't intersect. A Wall is the exception: although its artwork is a thin structural segment, it exclusively reserves its entire logical floor block, so no Rack or other room object can be placed or dragged into that cell and a Wall cannot be placed over any existing occupant. A 乖乖 pack dropped on a Rack's cell lands on the cabinet top and becomes that Rack's single rack-top 乖乖 Rack Device — the same shared package Rack View and the elevation rows always show centered above the cabinet; the floor plan and 2.5D view instead preserve the selected one of four rack-top corners and its selected facing — rather than a separate room object (the drop reads as blocked when the cabinet top already holds one); a pack placed on the floor stays a room object. In edit mode (`itops.floorPlan.blueprintEditHint` / `itops.floorPlan.isoEditHint`), Racks and objects are dragged between cells (dropping a Rack onto another Rack swaps the two, while a Wall cell rejects the drop); rotate, level, and remove controls appear only after the user clicks the Rack or room object to select it. Those controls rotate a facing (`itops.floorPlan.rotateTitle`), walk a quarter-block object to the next cell corner (`itops.floorPlan.cornerTitle`), move an object between vertical levels (`itops.floorPlan.raiseTitle` / `itops.floorPlan.lowerTitle`, tooltip `itops.floorPlan.objectLevel`), or remove it (`itops.floorPlan.deleteObjectTitle`); a placement with no free vertical span raises the `itops.floorPlan.objectNoSpace` Status Bar warning. In the 2.5D view an empty tile click (`itops.floorPlan.isoAddHere`) opens the New Rack dialog, and the selected Rack's delete control removes it with the usual confirmation. Unplaced cabinets default to one floor row per rack group, separated by aisles. The chosen layout, view angle, and zoom levels persist locally; the Server Room floor finish, grid placements, rack facings, and room objects are durable data stored in the app database (layouts saved before this existed are still read as a local fallback until the next edit). The Rack dialog previews the real rack elevation and stores `itops.racks.heightLabel`, `itops.racks.depthLabel` (millimetres), group, and shell properties. The Group field suggests groups already used in the selected Server Room while continuing to accept a new free-text group. Export offers `itops.export.pdf`.

The current 2.5D placement surface treats every rendered floor cell as a real target, including the extra cells that fill a large viewport. Its snapped preview is the same CSS-3D model that remains after placement. At 100%, the room fits its current pane and recalculates that baseline when the AI Assistant pane opens or a side panel changes width; the current pan stays relative to center so Racks remain inside the 2.5D clip box. The floor plan pans only when its current zoom or room bounds overflow the pane, preventing background gaps around a fully visible plan; the 2.5D view retains camera margin around its projected room. Both spatial canvases remain edge-to-edge when a Server Room background is active instead of inheriting the generic wallpaper content inset. In either view, drag blank floor with the left mouse button, drag with the middle mouse button, or focus the room and use the arrow keys to pan. Use the target button below the zoom levels, or click the middle mouse button without dragging, to reset the camera to center. Higher zoom levels expose the same controls over a larger scene. Right-clicking empty elevation or 2.5D space opens `itops.racks.changeBackground`; in Floor Plan mode the opaque blueprint grid remains edge-to-edge and that saved room background appears only behind the shared toolbar. Fixture face geometry and animation follow `Server Room Objects.dc.html`; the effective object facing combines the stored object rotation with the room view angle, so the reference design renders from all four fixed corners. Current one-cell footprints include CRAC at 0.94×0.62, UPS at 0.6×0.6, and crash cart at 0.56×0.44. The partition wall (`itops.floorPlan.object.wall`) is a floor-standing structure segment, 0.15 m thick against the 1200 mm cell and 2.4 m tall: it auto-connects to walls on the four orthogonally adjacent cells, drawing straight runs, corners, tees, and crosses in both spatial layouts, while an isolated wall renders as a straight open-ended run along its rotation axis (the rotate control turns it); its glyph and construction always follow the resolved connections, so dropping a wall next to an existing one joins them without further editing.

While the 2.5D room is hovered or focused, enter **Up, Up, Down, Down, Left, Right, Left, Right, B, A** within three seconds to trigger its blackout easter egg. Lightning cuts the room lights, 乖乖 packages remain visibly green, and rack lights return in shuffled order before the room resets automatically. This is decorative only: it changes no Server Room data or live operational state. Editable controls do not collect the sequence, and reduced-motion mode keeps the blackout while suppressing rapid flashes and flicker.

Room object gravity applies to resting objects: they land on the lowest fitting floor, cabinet, or object surface when placed or dragged. Top-hung fixtures such as cameras and detectors keep their overhead placement rules instead of falling to the floor.

Clicking a **Rack** opens **Rack View**. If only one mounting face contains devices, that elevation is centered with per-device "balloon" callouts; if both Front and Rear contain devices, both labeled elevations appear side by side, and Edit mode always shows both so either side is an obvious placement target. Each callout includes the Rack Device model and a single-line truncated notes preview when present. A rack-top 乖乖 appears above both elevations with front or rear artwork matching that face, but remains one shared Rack Device and therefore receives only one callout. When the Rack contains no Rack Devices, `itops.racks.emptyRackHint` links directly to Edit mode so the shared device picker becomes available. The Rack name and compact height/depth/device specifications sit in the top-middle toolbar, while the stage keeps only a small bottom inset and reserves visual room above the cabinet for a rack-top 乖乖 package. Rack View reduces the cabinet's per-U display height when the available window height is too short for the normal elevation, down to a legible floor, so common rack sizes avoid vertical scrolling whenever the screen permits. A breadcrumb climbs back up. Rack View edit mode enables Rack Device drag/drop between Front and Rear, empty-U drop targets, and a confirmation-backed delete control (`itops.racks.deleteItemTitle`) on each Rack Device. It also shows the shared right-side `itops.floorPlan.pickerTitle` column with searchable previews for every `itops.racks.kindLabel`; each card uses its faceplate text as the only visible type name and limits decorative ports or drive bays so that name has more room. Choosing one opens `itops.racks.addItemTitle` showing only that device's properties (no type switcher and no position field — placement happens next). Confirming with `itops.racks.placeAction` arms placement: the configured device remains visible under the mouse anywhere in the app content, then magnetically snaps to the nearest valid U on the chosen `itops.racks.mountingSideLabel` as the pointer approaches the cabinet. The snapped preview turns red where its span would overlap a placed device on that face, and the next valid left click places it. Right-click anywhere, press Escape, choose the armed picker card again, or navigate away to cancel. The picker column is the only add path in Rack View and the Server Room elevation layout alike: outside an armed placement, empty U slots are drag/drop targets only and clicking one does not open the add dialog. Choose `itops.racks.addServerRoom` to create an empty Server Room in a selected Site; creating a Server Room does not create a Rack. Choose `itops.racks.addRack` to add a Rack: `itops.racks.serverRoomSelectLabel` is required, so every Rack belongs to a Server Room in the selected Site.

Each Rack is drawn as an animated metal elevation: rail caps, a U-number gutter, and every device painted as its own faceplate that animates to match its kind and status (server fan spin and drive-bay LEDs, switch/router port blink, firewall throughput bars, disk-array grid, PDU load meter, UPS battery cells, KVM channels, and patch-panel ports). The Rack header shows online / warning / offline tallies, and devices slide in on load.

A **Rack Device** is added through the `itops.racks.addItemTitle` dialog, opened from the picker column's armed flow described above: in Rack View the placement click lands in the open Rack, and in the Server Room elevation layout it lands in the cabinet nearest the pointer. The compact two-column editor keeps identity and longer text in the first column — a live rack preview followed by label, model when applicable, notes, and tags — and gathers appearance and capacity properties with graphical shell-finish choices in the second. The `itops.racks.mountingSideLabel` Front/Rear segmented control chooses the structural mounting face independently of the Rack's floor-plan facing; changing an existing device's side re-validates its U span on the destination face. Capacity such as drive bays or ports shares one two-column row with device height. The type picker and position field are omitted — the type is already chosen on the picker card and the position comes from the placement click; editing an existing device also hides the type picker, since a placed device keeps its type. A Server also offers `itops.racks.formFactorLabel`: `itops.racks.formFactor.rack` uses the normal full rack width, while `itops.racks.formFactor.tower` draws a centered half-width tower chassis. A Switch, Router, or Generic Device offers `itops.racks.widthFractionLabel`: `itops.racks.widthFraction.full` spans the whole rack width, while `itops.racks.widthFraction.half` and `itops.racks.widthFraction.quarter` let two or four small devices (such as modems) share one U row side by side. Quarter-width Rack Device labels render at 70% of the standard font size in both the Rack and properties preview so more of the label remains visible. A companion `itops.racks.slotLabel` select picks the horizontal slot — `itops.racks.slotPosition.left` and `itops.racks.slotPosition.right` for half width, plus `itops.racks.slotPosition.centerLeft` and `itops.racks.slotPosition.centerRight` for quarter width — and during armed placement or drag/drop the slot instead follows the pointer's horizontal position across the row, with the preview turning red only when the targeted slot is taken on the same face. Beside it, `itops.racks.serverPanelStyleLabel` selects independently scalable artwork: `itops.racks.serverPanelStyle.default` keeps the original generic drive/fan face, `itops.racks.serverPanelStyle.style1` uses a dark drive wall with a silver hexagonal lattice, and `itops.racks.serverPanelStyle.style2` uses silver end bezels with converging grille rails and a green center indicator. Style 1 is height-aware rather than stretched: 3–4U uses a two-row lattice over a deeper drive wall; at 5U and taller, the lattice remains only on the upper faceplate and a separate lower expansion-card/grille chassis fills the remaining height. The live properties preview shows this transition through 5U. Required fields show `*`; unmarked fields are optional. Connection associations are managed separately through `itops.racks.bindingsAction`. A blue `itops.racks.boundHostCount` action at the bottom right opens the dedicated `itops.racks.hostBindingsTitle` editor. It binds the device to zero or one existing IT Ops Host, or creates and immediately selects a new Host; the Host's child Hosts remain represented through their parent and appear in the Rack View callout. The **Rack Device Type** (`itops.racks.kindLabel`) choice covers `itops.racks.kind.server`, `itops.racks.kind.storage`, `itops.racks.kind.switch`, `itops.racks.kind.router`, `itops.racks.kind.firewall`, `itops.racks.kind.pdu`, `itops.racks.kind.ups`, `itops.racks.kind.kvm`, `itops.racks.kind.patchPanel`, `itops.racks.kind.genericDevice`, and `itops.racks.kind.kuaiguai`.

Optional metadata includes notes, tags, and hardware model details. The **Ports** stepper controls the number of sockets drawn on a Switch or Router faceplate. Per-port inventory and SNMP settings are not exposed in the Rack Device editor; those metadata fields are reserved for future automatic SNMP discovery. None of these fields stores credentials or live Session state.

Rack Device position is managed only on the rack canvas: the common-properties editor has no Start U control, and placed devices are repositioned with drag/drop. In Rack View edit mode this includes dragging an already placed rack-top 乖乖 package into an available cabinet U and dragging an in-cabinet 乖乖 package back onto the rack top. The notes field shows three lines and the tags field one line by default.

Graphical PDF exports preserve localized names, labels, and other non-ASCII text through the platform's browser-font fallback instead of replacing those characters with question marks.

乖乖 is the only Rack Device that can be placed either inside the cabinet or on its top, and its editor shows only package properties — no status, shell finish, accent, or power draw. Its `itops.racks.kuaiguaiStyleLabel` choice offers two large-package poses: `itops.racks.kuaiguaiStyle.full` (standing, 4U) and `itops.racks.kuaiguaiStyle.laidDown` (package face upward, 1U). Rotation uses a slider, and expiry is selected with a date control on its own row. Moving its placement preview above the cabinet selects the rack-top position; one package may occupy that position, and the floor plan / 2.5D picker's 乖乖 dropped onto a cabinet top places this same rack-top package, so every view shows one shared object. Server Room floor views do not reveal 乖乖 packages stored inside cabinets, while a rack-top package remains visible on the cabinet footprint/top. When a package carries an `itops.racks.expiryLabel` date, its colors drain gradually over the final 30 days before that date, and on and after the date it renders fully black and white in every view that shows it; per-face dimming keeps the package erected in the 2.5D view. When the device's notes field is filled in, the large package's Rack View artwork (and the editor's live preview) scribbles the notes in a tiny handwriting style onto the ruled lines of the white note panel on the package's lower half — up to three lines, wrapping long text and ellipsizing the rest.

The AI Assistant can create the same rack-top package on one or more Racks. Its typed IT Ops tool first reads each Rack's height, then places kind `kuaiguai` at the Rack's virtual top position with the requested standing/laid-down style and optional expiry date; the normal one-package-per-top validation still applies.

Clicking a Rack Device that has bound Connections (its placed host and/or the `itops.racks.bindingsAction` bindings) opens a **connect popover** anchored to the faceplate. Each row shows the Connection's icon, name, and host with a `itops.racks.connectAction` action: terminals and file browsers connect in the background — a Status Bar notice (`itops.racks.connectOpenedNotice`) confirms the Session opened in the Workspace Module while IT Ops stays visible — and URL/RDP/VNC Connections switch to the Workspace Module, where those Sessions must be visible to start. A row whose Session is already open shows `itops.racks.goToSessionAction` with a live dot and jumps to its Workspace tab; the Activity Rail's IT Ops button returns to the same Rack View drill-down. A hover pencil edits a bound device; devices without bound Connections open the edit dialog on click. A placed Connection that no longer resolves to a Site member is shown dimmed with a `itops.racks.ghostBadge` badge — it cannot be opened but can still be edited or removed, and a deleted bound Connection renders as a `itops.racks.ghostBadge` row in the popover. Placement is a visual arrangement only; it does not modify the referenced Connections. The notes and additional bindings are inventory metadata that can support random Rack View callouts and multi-Connection device lookup without creating live Session state.

## Hosts

A **Host** is a durable inventory entry for one device or guest in a Site, addressed by hostname. The device itself can be a Host, and a Host can carry **child Hosts** — its virtual machines or containers (`itops.hosts.kind.physical`, `itops.hosts.kind.vm`, `itops.hosts.kind.container`, `itops.hosts.kind.other`). The Site-owned `itops.tabs.hosts` page lists the selected Site's Hosts as a parent/child tree with a count roll-up (`itops.hosts.hostCount`) and owns Host import and manual Task execution. Its `itops.hosts.taskStatusColumn` column shows queued, running, successful, or failed progress for the active Batch Run; `itops.hosts.lastRunStatusColumn` shows the newest completed result associated with the Host's bound execution Connection.

When the Site has no Hosts, the shared compact empty hint (`itops.hosts.empty`) includes an inline `itops.hosts.importAction` action. IT Ops uses the same no-glyph, one-sentence hint treatment for empty Hosts, Automations, Run History, Task Library, Site topology, Server Rooms, and Racks.

**Import.** `itops.hosts.importAction` opens a dialog (`itops.hosts.importTitle`) that accepts a pasted hostname list — one hostname per line, commas also separate, and `#` starts a comment (`itops.hosts.importHint`). Blank lines and duplicates are skipped; the Status Bar reports the outcome (`itops.hosts.importedNotice`). Imported Hosts are created as physical devices at the top level and are **scanned automatically** for remote-orchestration endpoints.

**Connectivity scan.** The scan probes each Host for SSH (port 22), WinRM (5985/5986), and HTTPS (443, a management-interface hint) and stores the snapshot on the Host — never live Session state and never a secret. Each row shows the detected endpoints as chips, `itops.hosts.scanning` while a probe runs, `itops.hosts.scanPending` for a never-scanned Host, or `itops.hosts.scanNone` when nothing answered. `itops.hosts.rescanAllAction` rescans the whole Site; each row's `itops.hosts.rescanAction` rescans one Host.

**Editing.** `itops.hosts.addAction` and each row's `itops.hosts.editAction` open the Host dialog (`itops.hosts.dialogTitleAdd` / `itops.hosts.dialogTitleEdit`) with `itops.hosts.hostnameLabel`, `itops.hosts.labelLabel`, `itops.hosts.kindLabel`, the `itops.hosts.parentLabel` select (`itops.hosts.parentNone` keeps it top level; a Host can never be its own ancestor), and `itops.hosts.notesLabel`. A row's `itops.hosts.addChildAction` pre-selects that Host as the parent. Deleting a Host asks for confirmation (`itops.hosts.deleteConfirmTitle`); its child Hosts are kept and move up one level, and no Connection is affected.

**Connection bindings.** Each row's link chip opens `itops.hosts.bindingsTitle`, where one Host binds any number of existing Connections — for example its SSH terminal Connection plus an HTTPS URL Connection to its management interface (`itops.hosts.bindingsHint`). Bindings are ordered soft references; deleting a Connection never deletes the Host.

**Rack View callouts.** The Rack Device editor's `itops.racks.boundHostCount` action opens a dedicated Host-binding editor for the Host the device *is*; that device's balloon callout in Rack View then lists the Host's hostname and its child Hosts (`itops.hosts.childOverflow` summarizes the rest), so a rack slot reads as "this machine, and the guests running on it".

## Batch Runs and Run History

A **Batch Run** executes a reusable Task or ad-hoc Script Batch Task across resolved targets. It is an execution event, not an independent page. On the Site-owned Hosts page, select one or more Hosts with an SSH Connection binding and choose `itops.actions.runTask`. The launcher keeps that Host selection fixed, shows it with `itops.batchRuns.scopeSelectedHosts`, and lets you choose a global reusable Task with `itops.batchRuns.taskSourceLabel` or enter a one-off Script with `itops.batchRuns.adHocTask`. Selecting a saved Task shows its Script or Playbook definition in the same editor-style code or node-canvas presentation used by the Task Library, but read-only; the ad-hoc Script remains editable. Choose `itops.actions.run` to start the Batch Run. For each selected Host, resolution uses its first bound SSH Connection; a Host without an SSH binding cannot be selected for a run.

The separate `itops.navigation.runHistory` page shows the selected Site's live run and completed reports. It is an audit surface with no start or rerun action; manual execution begins from Hosts, while Automations can also create runs.

Before any run exists, `itops.batchRuns.historyEmptyHint` explains where Run History comes from without presenting a setup action on this read-only page.

## Task Library

A **Task** is a reusable script or Playbook definition; it does not own targets. Open the global `itops.tasks.heading` destination to create, edit, or delete either kind. `itops.tasks.applicableOsLabel` accepts one or more supported operating systems; `itops.tasks.applicableOs.any` is exclusive with specific platforms. The metadata documents compatibility and filters the library, but does not automatically remove selected Hosts from a run. The spreadsheet-style list shows each Task's type, Applicable OS, execution count, failed-host count, and `itops.tasks.viewHistory` link. The link opens the most recent Site Run History attributed to that stable Task id; older or ad-hoc rows are not guessed from their labels. Manual execution starts from selected Hosts with `itops.actions.runTask`; the launcher then offers definitions from the Task Library. Deleting a Task does not change saved Run History.

The Task Library includes app-owned, inspection-only built-ins for Linux, macOS, Windows, Cisco IOS, Cisco NX-OS, FortiOS, Juniper Junos, and Arista EOS. They cover identity, uptime, resource usage, interfaces, routing and DNS, and recent logs. A built-in row shows `itops.tasks.builtInBadge`; choose `itops.tasks.duplicateBuiltin` to create an editable user copy. Built-ins themselves cannot be edited or deleted and are refreshed when the catalog changes.

The reusable Playbook editor uses an ordered workflow canvas similar to the Automation editor. Choose the `itops.tasks.addNode` plus button on any connector, then choose `itops.tasks.addCommand`, `itops.tasks.addSudo`, or `itops.tasks.addAi` from the popup to insert that node at the connector's exact position. The bottom add-node bar appends the same node types to the end of the chain. Select a node to edit it in the inspector. A Sudo node stores its password in the configured secret vault and keeps only an opaque reference in the Task; credential typing stays isolated from canvas rendering and `itops.tasks.credentialStoredDetail` confirms durable state. At run time KKTerm opens one interactive SSH shell per Host, validates sudo once, and lets the remote sudo timestamp apply to the remaining nodes in that run. The password is not stored in Task JSON or Run History.

Add `itops.tasks.addAi` after a node whose output needs interpretation. The AI node sends the preceding node output to the currently configured AI Assistant provider as untrusted data and applies the instruction in `itops.tasks.aiInstructionLabel`. It accepts only three structured results: `continue` runs the next node, `success` ends that Host successfully, and `fail` stops that Host as failed. Tools are disabled for this evaluation, and model output is never executed as a command. Provider errors or malformed decisions fail that Host. Because the previous output leaves the device for the configured provider, do not place an AI node after a command that prints secrets or other data that must remain local.

Execution fans out with bounded concurrency. The live view shows queued, running, successful, and failed hosts and streams each host's combined output. `itops.actions.cancel` stops an active run. A completed run is written to Recent runs; select an entry to open its read-only Run Report and inspect the saved per-host output.

The script is state-changing operator input. Review it and the target Site before starting. Authentication secrets remain in the OS keychain and are not copied into the Site or report.

## Automations

An **Automation** is a durable trigger → optional condition → ordered actions rule. Its saved definition persists and enabled rules re-arm when KKTerm launches; the live **Watchdog** runtime performs sampling and execution in memory.

Each Automation carries a durable Site binding, chosen with the `itops.editor.siteLabel` select in the editor header (`itops.editor.siteNone` leaves it unbound). The Site-owned `itops.tabs.autos` page lists the Automations bound to the selected Site; an Automation created there is bound to it by default. Rules saved before the binding existed show where they can be inferred: a Batch Run action targeting that Site, or a host-addressed trigger (ping / TCP reachable) watching one of the Site's resolved member hosts. `itops.actions.newAutomation` opens the editor. In the editor, name the rule, configure its trigger and condition, then add typed actions with `itops.actions.addAction`. Available actions include notify, popup, email, webhook, and running a Batch Run. The editor's Test action samples/evaluates the rule and previews actions without sending email, calling a webhook, or starting a Batch Run. Save with `itops.actions.create` or `itops.actions.save`.

When none are bound to the Site, `itops.automations.emptyHint` uses the shared compact empty-hint treatment and links inline to the New Automation flow.

The Automations list shows `itops.automations.armed` or `itops.automations.disabled`. Use its toggle to arm or disarm a rule, or use the edit and delete actions. Triggered in-app notifications use the Status Bar; popup actions use an app-owned dialog. The Watchdog Status Bar indicator provides app-wide live runtime visibility.

## AI Assistant

The shared AI Assistant receives compact IT Ops page context: the current navigator selection, Site names/member counts/transports, loaded rack-topology counts, Automation names and armed state, the Task Library count, recent-run count, and a live-run summary. It does not receive scripts, host output, secrets, Rack Device details, or full trigger/action bodies through page context. Ask it operational questions normally; for UI guidance it searches this chapter and can navigate to the IT Ops Module — including a specific Site's Hosts, Automations, or Run History destination and the global Task Library — or highlight one of the tutorial targets above after you ask to be shown. It can also highlight one specific row using an entity target (`itops.site:` / `itops.host:` / `itops.automation:` / `itops.task:` / `itops.run:` followed by the entity id).

Beyond reading, the assistant's approval-gated `itops_*` tools cover the Module's operations: managing Sites, Server Rooms, Racks, and Rack Devices; creating, importing, scanning, editing, and deleting Hosts; reading and editing the Task Library (built-ins stay read-only, and sudo credentials can only be configured in the Task editor); creating, testing, arming, and deleting Automations; and starting or canceling Batch Runs plus reading Run History reports. Every mutating call goes through the standard tool-approval prompt unless the tool permission mode is Allow All; the assistant can never run a site task silently.
