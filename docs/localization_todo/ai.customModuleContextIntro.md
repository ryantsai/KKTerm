# `ai.customModuleContextIntro`

English: `The user is viewing this Custom Module. Use the module metadata below as page context.`

First line of the AI page context sent with prompts while a Custom Module
page is active. Best-effort translations exist in the locale files; keep the
pending file until a verified localization pass completes per
`docs/localization_todo/README.md`.
