# `ai.customModuleContextLabel`

English: `Custom Module page context`

Shown in the AI Assistant panel context line while a Custom Module page is
active. Best-effort translations exist in the locale files; keep the pending
file until a verified localization pass completes per
`docs/localization_todo/README.md`.
