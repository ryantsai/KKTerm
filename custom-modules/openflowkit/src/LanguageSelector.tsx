import type React from 'react';

interface LanguageSelectorProps {
  variant?: 'default' | 'compact' | 'minimal';
  placement?: 'top' | 'bottom';
}

export const LanguageSelector = (
  _props: LanguageSelectorProps,
): React.ReactElement | null => null;
